<UserControl x:Class="Bongruel.LoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bongruel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <!--
        <ProgressBar Width="300" Height="35" Minimum="0" Maximum="100"
                     Value="{Binding WorkerState}"></ProgressBar>
        -->
        <Image x:Name="loadImg" HorizontalAlignment="Center" VerticalAlignment="Center" Source="Assets/spinner-of-dots.png" Width="200"/>
        <TextBlock Text="Loading" VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="24" />
    </Grid>
</UserControl>
